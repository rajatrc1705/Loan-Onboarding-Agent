# Agent Service

This folder will house the LiveKit agent service.

We will refactor the existing `agent.py` into a runnable service here and add
an HTTP trigger so the backend can request the agent to join a room.
